ref_list is based on C++ code of WebRTC M97
it's  for your convenience, so that you don't need to build your ref-lists.
fast-find can also build all kinds of ref-list. So the ref-list here is also a reference for you.
anyway, you can also ignore this, and use the list build by fast-find itself